"use strict";

module.exports = {

  execute: function (params, req, cb) {

    /*
     * Do some logic here, like query db based on params or req.
     */

    cb(null, {
      template : 'widget.br',
      data : params
    });
  }
}